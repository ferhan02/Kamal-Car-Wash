Place your generated car pattern background image in this folder using this exact filename:

car pattern bg.png

The shared CSS already references:
../images/car pattern bg.png

Light mode uses a stronger wash over the pattern so it stays subtle.
Dark mode uses an even darker wash so the pattern does not compete with content.
